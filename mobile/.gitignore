# Dependencies
node_modules/

# Expo
.expo/
.expo-shared/
dist/
web-build/
expo-env.d.ts.bak

# Native projects generated by `expo prebuild` (managed workflow: not committed)
/android/
/ios/

# Metro / Jest
.metro-health-check*
coverage/
*.tsbuildinfo

# Local env (repo-root .gitignore already ignores .env / .env.* and
# un-ignores .env.example; this file adds nothing that would weaken that)

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# macOS
.DS_Store
